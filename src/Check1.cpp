#include <cstdlib>
#include <iostream>
#include <string>

using namespace std;

int Check1(string* V2,
        string* V3,
        string* List1,
        string* List2,
        int* i);
{
    if (Check == ListI[i]) {
        cout << ListI[i] << " -- " << ListV2[i] << " -- " << ListV3[i] << " -- "
             << ListT[i] << endl
             << endl;
        flag = true;
        break;
    }
    return flag;
}